# academy_pratica_e1
void main() {
  const v1 = 9.0;
  const v2 = 6.7;
  const v3 = 2.0;
  const v4 = 4.7;
  const media = (v1 + v2 + v3 + v4) / 4;

  print(
    'A media entre [ ${v1.toStringAsFixed(1)} , ${v2.toStringAsFixed(1)} , ${v3.toStringAsFixed(1)} , ${v4.toStringAsFixed(1)} ] = ${media.toStringAsFixed(1)}',
  );
}
